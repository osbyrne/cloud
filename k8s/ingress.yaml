apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: contact-manager-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: contact-manager-svc
                port:
                  number: 80
